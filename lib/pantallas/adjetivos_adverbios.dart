import 'package:flutter/material.dart';

class AdjetivosAdverbiosScreen extends StatelessWidget {
  const AdjetivosAdverbiosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Adjetivos y Adverbios")),
      body: const Center(child: Text("Pantalla de adjetivos y adverbios")),
    );
  }
}
