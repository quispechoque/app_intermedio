import 'package:flutter/material.dart';

class PracticaBe extends StatelessWidget {
  const PracticaBe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Práctica TO BE")),
      body: const Center(
        child: Text("Aquí irá la práctica del verbo TO BE"),
      ),
    );
  }
}
